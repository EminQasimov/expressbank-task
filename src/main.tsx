import * as React from 'react'
import * as ReactDOM from 'react-dom'

import ExpressbankApp from '~/app'

const root = document.getElementById('root')

ReactDOM.render(
  <React.StrictMode>
    <ExpressbankApp />
  </React.StrictMode>,
  root,
)
